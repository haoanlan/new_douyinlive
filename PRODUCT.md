# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要使用者：项目所有者本人，单人使用。** 场景是服务器上 7×24 采集抖音直播数据，
本人通过浏览器远程查看与管理。使用者会**在手机上**查看。

**次要使用者（未定）**：可能会让其他人登录查看数据——只读用途，不参与监控配置。
代码里已有 `R_SUPER` / `R_GUEST` 两种角色，但权限区分尚未真正实现。

## Product Purpose

监控指定抖音直播间的实时状态与观众行为：房间开播时自动建立 WebSocket 长连接，
记录弹幕 / 礼物 / 进场 / 在线数据并落库 SQLite，下播时产出汇总与图片报告（可发飞书）。

成功意味着：**无需人工盯守也能完整留存每场直播的数据**，并且随时能查清
「现在有没有在播、有没有录上」以及「某场直播 / 某个人 / 某个主播的数据是什么」。

## Positioning

完全自建：数据落在本机 SQLite，通过自写的 Go 抓取代理直连抖音，不依赖第三方 SaaS
数据服务。**这一价值主张尚未由使用者明确确认，暂记为弱定位。**

## Operating Context

- **部署**：Linux 服务器常驻运行（`start.sh` 指向 `/opt/data/douyin-monitor`），
  由浏览器远程访问；开发环境在 Windows。
- **采集链路**：自写 Go 抓取代理（端口 1088）为每个房间提供一条 WebSocket；
  Node 侧守护进程负责连接、解析与落库。**抖音 cookie 目前未配置**，
  因此部分主播资料（昵称/头像/粉丝数）拿不到是常态。
- **被监控的房间经常全部处于未开播状态** —— 这是常态而非异常。
- **数据规模**：弹幕已 27 万条以上、礼物 23 万条以上，且会持续增长。
- 使用者会在手机上查看。

## Capabilities and Constraints

**已确认能力**：房间增删改查与暂停/恢复；实时状态监控；场次历史；弹幕/礼物/进场明细；
礼物与主播排行；词云与趋势；礼物流水时间线；匿名查询（按昵称跨主播查行为）；
用户画像；图片报告与飞书通知；CSV 导出。

**约束**：
- 单机部署、数据持续增长 → 不允许全量拉取与全量渲染。
- 远程浏览器访问 → 网络可能慢，轮询频率与首屏体积需要克制。
- 部分房间常年未开播、部分字段永远拿不到 → 空态与缺字段是主要视觉状态。
- 手机访问 → 布局与触控目标需要真实可用。

**未决事项**：是否需要真正的只读访客角色；是否配置抖音 cookie。

## Evidence on Hand

真实数据可用：本地 SQLite（`db/douyin.db`）含 27 万+ 弹幕、23 万+ 礼物与上百个场次，
可直接用于真实数据规模下的界面验证。**不要编造示例数据。**

## Product Principles

1. **「现在」与「历史」必须分开。** 使用者同时需要实时状态、单场复盘与人脉分析，
   三者没有主次，因此靠分层与页内导览解决，而不是把三类信息平铺成等权重卡片。
2. **常态是空的，不是坏的。** 房间常年未开播、部分字段永远缺失；空态与缺字段必须
   表达为正常，把「故障」的视觉权重留给真正的故障。
3. **失败绝不伪装成空或正常。** 请求失败、轮询失败、数据过期都必须可见——
   监控工具最不可接受的失败模式是让人以为一切正常。
4. **数据规模是一等约束。** 任何列表、图表、导出都必须能在 27 万+ 行下工作。
5. **单人效率优先，但为只读访客留门。** 默认界面服务于单个人的盯播效率；
   只读查看者不应承担配置类操作的风险。

## Accessibility & Inclusion

使用者确认会在手机上查看，且这是长时间注视、需要快速扫读的监控界面。
已知问题（来自 `docs/DESIGN-REVIEW.md`）：主要交互大量使用裸 `div @click` 与
无标签的图标按钮，键盘不可达；12px 辅助文字对比度低至 1.3:1–3.55:1；
状态仅靠颜色表达。目标标准：正文达 WCAG AA（4.5:1）、键盘可达、状态不只靠颜色。
